cors:
  originPatterns: url
spring:
  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/spring_boot_erudio?useTimezone=true&serverTimezone=UTC
      username: root
      password: 12345
  jpa:
    open-in-view: false 
    hibernate:
      ddl-auto: update
    show-sql: true
    # properties:
    #  hibernate:
     #   dialect: org.hibernate.dialect.MySQLDialect